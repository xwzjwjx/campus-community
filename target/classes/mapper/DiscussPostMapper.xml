<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wjx.community.dao.DiscussPostMapper">

    <sql id="selectFields">
        id, user_id, title, content, type, status, create_time, comment_count, score
    </sql>

    <sql id="insertFields">
        (user_id, title, content, type, status, create_time, comment_count, score)
    </sql>
    <insert id="add" parameterType="DiscussPost">
        insert into discuss_post <include refid="insertFields"></include>
        values (#{userId}, #{title}, #{content}, #{type}, #{status}, now(), #{commentCount}, #{score})
    </insert>


    <select id="selectAllDPRows" resultType="java.lang.Integer">
        select
            count(id)
        from
            discuss_post
        where
            status != 2
        <if test="userId != 0">
            and user_id = #{userId}
        </if>
    </select>
    <select id="selectAll" resultType="DiscussPost">
        select
               <include refid="selectFields"/>
        from
             discuss_post
        where
            status != 2
        <if test="userId != 0">
            and user_id = #{userId}
        </if>
        order by type desc, create_time desc
        limit #{offset}, #{limit}
    </select>
    <select id="selectOneById" resultType="com.wjx.community.entity.DiscussPost">
        select
            <include refid="selectFields"></include>
        from discuss_post
        where id = #{discussPostId}
    </select>
</mapper>